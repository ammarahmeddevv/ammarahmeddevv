{
  "name": "ammarahmeddevv-profile",
  "private": true,
  "type": "module",
  "description": "Source for the github.com/ammarahmeddevv profile README. Data in data/, presentation in scripts/, output in README.md + assets/.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "sync:ai": "node scripts/sync-ai.mjs",
    "check:contrast": "node scripts/check-contrast.mjs",
    "check": "npm run check:contrast && npm run build",
    "sync:github": "node scripts/sync-github.mjs"
  },
  "dependencies": {
    "opentype.js": "^1.3.4"
  }
}
